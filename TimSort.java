import java.util.Arrays;

public class TimSortExample {
    public static void timSort(int[] arr) {
        Arrays.sort(arr);  // TimSort is used internally
    }
}

// Java's built-in sort method is based on TimSort, so you don't need to implement it manually. You can use Arrays.sort().